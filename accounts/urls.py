from django.conf.urls import url
from . import views

urlpatterns = [
    #url(r'^login/$', views.formlogin, name='login'),
    #url(r'^logout/$', 'django.contrib.auth.views.logout', name='logout'),
]